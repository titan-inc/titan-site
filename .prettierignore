# Gerados — o Prettier não deve tocar
pnpm-lock.yaml
node_modules/
dist/
.next/
coverage/
next-env.d.ts

# Escritos pelo Yaak (app/CLI) via Local Directory/Git Sync — ver
# yaak/README.md. Reformatar à mão entraria em cabo de guerra com o próprio
# Yaak, que reescreve no formato dele a cada sync. README.md da pasta
# continua formatado normalmente, só o YAML gerado é ignorado.
yaak/*.yaml

# Gerado pelo create-next-app, com marcadores próprios
apps/web/AGENTS.md

# Contrato aprovado: formatação é conteúdo e só muda com autorização explícita
docs/landing/04-implementation-spec.md

# Fora do git, mas dentro do glob do Prettier — que não lê .gitignore nem
# .git/info/exclude. Sem estas linhas o `format:check` reprova mesmo com o
# repositório limpo, e um comando que sempre falha deixa de verificar.
#
# O escopo em .claude/ é estreito de propósito: um settings.json versionado e
# compartilhado deve continuar sendo formatado como qualquer JSON do repo.
localdocs/
.claude/settings.local.json
