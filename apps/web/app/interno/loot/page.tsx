import { EmImplementacao } from './_components/em-implementacao';

export const metadata = { title: 'Loot — Titan Inc' };

/**
 * Aba da sessão corrente. Conteúdo é M5 — ver TIT-62 a TIT-69.
 *
 * É a aba inicial de propósito: durante a raid, "o que está rolando agora" é a
 * única pergunta que tem pressa.
 */
export default function LootSessaoPage() {
  return (
    <EmImplementacao>
      Aqui vai ficar a sessão de loot em andamento, e é daqui que um oficial abre uma nova.
    </EmImplementacao>
  );
}
